// import { fromJS } from 'immutable';
// import { makeSelectWorkSpacePageDomain } from '../selectors';

// const selector = makeSelectWorkSpacePageDomain();

describe('makeSelectWorkSpacePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
