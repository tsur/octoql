/**
 *
 * App Wrappers
 */

/* System imports */
import styled from 'styled-components';

export const Span = styled.span`
  :root {
    --langColor: inherit;
  }

  .status_bar_lang {
    color: var(--langColor);
  }
`;
